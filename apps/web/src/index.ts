/**
 * `@neko/web` — the client.
 *
 * Playback (Phase 2) and the single-player run (Phase 4) live here. The
 * React application, input handling, rendering and multiplayer client
 * state are still at the repository root in `src/`, and move here as
 * their phases land.
 */
export * as playback from './playback/index.ts';
export * as play from './play/index.ts';

